using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SimpleSp : MonoBehaviour
{
    public List<SimpleSp> sps;
    // Start is called before the first frame update
    void Start()
    {
        //GetComponent<SpriteAnimation>.SetSprite(sps, 0.1f, false);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
